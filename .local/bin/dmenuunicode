#!/bin/sh
chosen=$(grep -v "#" ~/.config/emoji | dmenu -i -l 20 -fn Monospace-18)
[ "$chosen" != "" ] || exit

c=$(echo "$chosen" | sed "s/ .*//")
sleep 0.05s
echo $c | str2ev >> /dev/input/by-path/platform-i8042-serio-0-event-kbd
